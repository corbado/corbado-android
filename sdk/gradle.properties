# Maven coordinates for :sdk (com.corbado:connect-core). VERSION_NAME is the version of the
# next release; the release workflow (.github/workflows/release.yml) requires the v<semver>
# tag to match it and passes it explicitly via -PVERSION_NAME.
GROUP=com.corbado
POM_ARTIFACT_ID=connect-core
VERSION_NAME=1.4.1
